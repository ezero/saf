@model Activosfijos.Models.ingresoegreso

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ingresoegreso</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.idingresoegreso)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.idingresoegreso)
            @Html.ValidationMessageFor(model => model.idingresoegreso)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.tipoingreso)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.tipoingreso)
            @Html.ValidationMessageFor(model => model.tipoingreso)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.tipoegreso)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.tipoegreso)
            @Html.ValidationMessageFor(model => model.tipoegreso)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
