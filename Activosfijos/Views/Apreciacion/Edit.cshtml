@model Activosfijos.Models.apreciacion

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>apreciacion</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.idapreciacion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.idapreciacion)
            @Html.ValidationMessageFor(model => model.idapreciacion)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.porcentajeapreciacion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.porcentajeapreciacion)
            @Html.ValidationMessageFor(model => model.porcentajeapreciacion)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
