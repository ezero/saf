@model Activosfijos.Models.depreciacion

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>depreciacion</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.iddepreciacion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.iddepreciacion)
            @Html.ValidationMessageFor(model => model.iddepreciacion)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.porcentajedepreciacion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.porcentajedepreciacion)
            @Html.ValidationMessageFor(model => model.porcentajedepreciacion)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
