@model IEnumerable<Activosfijos.Models.tipocambio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idtipobien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.gestion.idgestion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.moneda.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idtipobien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.gestion.idgestion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.moneda.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
