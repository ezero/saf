@model IEnumerable<SAF.Models.Gestion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GestionFecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GestionFecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GestionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GestionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GestionId })
        </td>
    </tr>
}

</table>
