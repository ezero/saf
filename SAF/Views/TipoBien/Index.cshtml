@model IEnumerable<SAF.Models.TipoBien>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoBienDescripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoBienDescripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoBienID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoBienID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoBienID })
        </td>
    </tr>
}

</table>
